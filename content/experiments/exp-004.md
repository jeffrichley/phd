---
# The first row on this ledger that can become false with nobody editing anything. A done row
# cannot rot; a queued one rots untouched. `queued_on` is what the row renders as its stamp, so
# the staleness is visible on the page rather than inferred. See phd-lab#79.
id: exp-004
date: 2026-09-08
queued_on: 2026-09-08
rq: rq2
hypothesis: H2.1
env: "Skill-library platform, ten-seed baseline retrain on HPC allocation"
method: "Baseline retrain at ten seeds, the denominator study 2's forward-transfer comparison is measured against. Build tickets cut 2026-09-08; the run follows the platform build"
seeds: 10
status: queued
commit: ""
metrics: {}
artifacts: []
---

## Reading

Recorded before it runs, with its id assigned in advance, because a ledger that only gains
rows after the fact cannot be checked against a plan. §06 stage 03 already commits to this
run; without a row here the two pages disagree about whether the program is in motion.

Nothing is claimed yet. The row exists to be falsifiable: if it is still queued well past the
window it was entered against, that is a fact about the schedule and the stamp on the row is
what makes it visible.
